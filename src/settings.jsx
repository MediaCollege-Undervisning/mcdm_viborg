// Fælles indstillinger for hele projektet.
// Ét sted at ændre serveradresse o.l., så vi ikke gentager den i hver loader.

// Adressen på API'et, som loaderne henter data fra.
// TODO (code-along): ret til jeres eget endpoint, når API'et er på plads.
export const serverPath = `http://localhost:3042`;
